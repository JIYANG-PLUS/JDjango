class ${view_name}(View):

    TEMPLATE = ''

    def get(self, request):
        ARGS = {}
        ARGS['obj'] = None
        return render(request, self.TEMPLATE, ARGS)
    
    def post(self, request):
        keys = request.POST.get('searcher', '').strip()
        return redirect('/')